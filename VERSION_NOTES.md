# 代码版本与交接记录

制作/最终分区与可点击标签见[版本导航](docs/versions/README.md)。目前无最终交付版；`main`仍是制作分支。

Git历史从2026-08-31首次导入开始。此前工作记录在工作区根目录的制作日志中，不能把本次导入说成过去已存在的Git提交。

| 标签 | 内容 | 边界 |
|---|---|---|
| `process/v0.3.0-preview` | 公众文案分层、解释型图解、6—8镜、问题自动检索入口及代码索引 | 制作预览；来源范围有限；通用有声卡通MP4尚未完成，见独立变更说明 |
| `v0.2.0-studio` | 跨主题资料隔离与独立项目、Qwen生成/审核/修订/复检、可视化修改记录、草稿ZIP和单端口本地启动 | 通用视频目前是独立分镜及无声预演，尚无自动MP4/配音；AI审核不代替科学终审；未公网部署 |
| `v0.1.0-baseline` | 导入现有海报原型、太阳有声样片、自动字幕/时长修正工作台；加入敏感文件排除与干净测试夹具 | 非任意主题全自动视频生成；没有公网部署 |
| `v0.1.1-handoff` | 补充GitHub建仓、接手运行、恢复版本与部署门槛说明 | 不含API密钥、在线知识库或私有原始成果档案 |
| `v0.1.2-handoff-check` | 根据新目录恢复测试补正“先构建前端，再跑后端测试”的顺序，记录实际验证环境 | 应用功能与上一版本相同；不伪装为新增功能版本 |

后续每个经过验证的功能形成新提交；重要里程碑增加新标签。不要移动已有标签、倒填提交时间或覆盖旧备份。代码仍未上传GitHub时，本地版本和离线备份可用于恢复，但不等同远端备份。

当前Git作者使用项目级临时身份 `SciVis Local Backup <scivis-backup@local.invalid>`，仅限本仓库，不代表用户的GitHub身份。接手者可在仓库内设置自己的提交姓名和邮箱；不要因此重写已有历史。

版本查看和安全恢复示例：

```powershell
git log --oneline --decorate
git tag
git switch -c review-baseline v0.1.0-baseline
```

切换前先保存当前改动；需要独立试验时优先从备份克隆到新目录，不要对工作目录执行强制重置。
