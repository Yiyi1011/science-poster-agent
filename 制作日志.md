# 制作日志

> 按时间记录操作、错误与解决办法，供回溯与赛事提交引用。费用以 `evidence/model-usage.jsonl` 台账为准（估算值，非实付）。

## LOG-001 v0.5 通用闭环开发（2026-09-01，dev/general-science-video-v0.5）

### 基线
- 从 `process/v0.4.5-handoff` 创建分支 `dev/general-science-video-v0.5`。
- 基线验证：后端 163 项测试通过，前端 `vite build` 通过（v0.5 首个提交 `543800d`）。

### 闭环加固实现（对应简报 §6）
- JSON 安全解析：`studio_structured_output.py` 字符串感知括号扫描、拒绝多义 JSON、仅修复 trailing comma、容器内别名修复。
- 确定性 6 镜 fallback：`studio_fallback.py`，明确标注 `fallback` 不冒充模型输出；讲解节加长至 ≥60 字符避免自触发充分性警告。
- 检索后备：`DOMAIN_BACKSTOPS`（NASA/IES/WHO）+ 关键词拆分/扩展只在初始不足时补充，控制调用次数。
- 来源选择部分支持规则：来源只能支撑部分核心主张时仍应选择并如实写 gap，不得靠模型知识补写证据。
- SQLite 锁加固：WAL 模式 + `timeout=30`，应对 WPS 云同步间歇性文件锁。

### 验收题一：月亮题（项目 1fd588fc）
- research→生成(8镜)→两轮审核→人工反馈→v4 clean→媒体 3 次尝试后成功：90.64 秒 MP4，8/8 镜，完整性检查通过。
- 问题与解决：NASA moon/ 首页缺机制文本致 11 条 blocker → 补 moon-phases 后备页；第 6 镜 8 相循环两次被视觉拒 → 重试规划成功后通过。

### 验收题二：间隔学习（项目 8c34e0dd）
- 审核中两轮人工反馈收敛至 v6 clean（0 findings，run succeeded）。
- 媒体第 5 镜（判断掌握×小测验，exchange 关系）两次被视觉检查拒绝：双向箭头画在两条错开的水平线上，被读成单向/歧义。
- 修复：`studio_cartoon.py` 的 `frame()` 将 exchange 改为同一水平线两端各画箭头（◄──►），流动圆点分上下两行按方向对向运动、颜色区分。41 项相关测试通过。
- 重试媒体任务成功：68.0 秒 MP4，7/7 镜，1—4 镜旁白复用旧任务避免重复扣费。

### 验收题三：AI 题（项目 1e0d3746）
- 既有 v3 与媒体（68 秒 MP4）复核：独立 `verify_media_output`（ffprobe 时长/音轨 + 3 帧抽帧）通过。

### 验收题四：API 题（项目 86b6280c）
- v2 已通过审核但无媒体；补跑卡通媒体任务：8/8 镜一次通过（exchange 渲染修复对全部双向箭头场景生效），68.0 秒 MP4，完整性复验通过。

### 费用记录
- 全台账估算累计约 13.44 元（含本会话前 v0.3/v0.4 各轮真实调用）；今日新增约 4.45 元（月亮题 + 间隔学习 + API 题的真实生成、审核与媒体调用）。
- 远低于 70 元暂停线/100 元实付上限；真实账单以百炼控制台为准，`evidence/billing-snapshot-*.json` 与 `evidence/model-usage.jsonl` 留档。

### 遗留与后续
- 视频内容本身尚未做受众测试与领域专家终审，不得写为“已通过专家认证”。
- 间隔学习与 API 题的版本状态为 `ai_checked_human_pending`，按简报流程留人工核查入口。
