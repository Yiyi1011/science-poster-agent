# 制作日志

> 按时间记录操作、错误与解决办法，供回溯与赛事提交引用。费用以 `evidence/model-usage.jsonl` 台账为准（估算值，非实付）。

## LOG-001 v0.5 通用闭环开发（2026-09-01，dev/general-science-video-v0.5）

### 基线
- 从 `process/v0.4.5-handoff` 创建分支 `dev/general-science-video-v0.5`。
- 基线验证：后端 163 项测试通过，前端 `vite build` 通过（v0.5 首个提交 `543800d`）。

### 闭环加固实现（对应简报 §6）
- JSON 安全解析：`studio_structured_output.py` 字符串感知括号扫描、拒绝多义 JSON、仅修复 trailing comma、容器内别名修复。
- 确定性 6 镜 fallback：`studio_fallback.py`，明确标注 `fallback` 不冒充模型输出；讲解节加长至 ≥60 字符避免自触发充分性警告。
- 检索后备：`DOMAIN_BACKSTOPS`（NASA/IES/WHO）+ 关键词拆分/扩展只在初始不足时补充，控制调用次数。
- 来源选择部分支持规则：来源只能支撑部分核心主张时仍应选择并如实写 gap，不得靠模型知识补写证据。
- SQLite 锁加固：WAL 模式 + `timeout=30`，应对 WPS 云同步间歇性文件锁。

### 验收题一：月亮题（项目 1fd588fc）
- research→生成(8镜)→两轮审核→人工反馈→v4 clean→媒体 3 次尝试后成功：90.64 秒 MP4，8/8 镜，完整性检查通过。
- 问题与解决：NASA moon/ 首页缺机制文本致 11 条 blocker → 补 moon-phases 后备页；第 6 镜 8 相循环两次被视觉拒 → 重试规划成功后通过。

### 验收题二：间隔学习（项目 8c34e0dd）
- 审核中两轮人工反馈收敛至 v6 clean（0 findings，run succeeded）。
- 媒体第 5 镜（判断掌握×小测验，exchange 关系）两次被视觉检查拒绝：双向箭头画在两条错开的水平线上，被读成单向/歧义。
- 修复：`studio_cartoon.py` 的 `frame()` 将 exchange 改为同一水平线两端各画箭头（◄──►），流动圆点分上下两行按方向对向运动、颜色区分。41 项相关测试通过。
- 重试媒体任务成功：68.0 秒 MP4，7/7 镜，1—4 镜旁白复用旧任务避免重复扣费。

### 验收题三：AI 题（项目 1e0d3746）
- 既有 v3 与媒体（68 秒 MP4）复核：独立 `verify_media_output`（ffprobe 时长/音轨 + 3 帧抽帧）通过。

### 验收题四：API 题（项目 86b6280c）
- v2 已通过审核但无媒体；补跑卡通媒体任务：8/8 镜一次通过（exchange 渲染修复对全部双向箭头场景生效），68.0 秒 MP4，完整性复验通过。

### 费用记录
- 全台账估算累计约 13.44 元（含本会话前 v0.3/v0.4 各轮真实调用）；今日新增约 4.45 元（月亮题 + 间隔学习 + API 题的真实生成、审核与媒体调用）。
- 远低于 70 元暂停线/100 元实付上限；真实账单以百炼控制台为准，`evidence/billing-snapshot-*.json` 与 `evidence/model-usage.jsonl` 留档。

### 遗留与后续
- 视频内容本身尚未做受众测试与领域专家终审，不得写为“已通过专家认证”。
- 间隔学习与 API 题的版本状态为 `ai_checked_human_pending`，按简报流程留人工核查入口。

## LOG-002 M2 主工作台 UI 优化（2026-09-01，dev/general-science-video-v0.5）

### 变更内容（提交 `06ff3c1`）
- 术语本地化：证据/调用记录/失败类型从原始英文 ID 改为中文（"第N条事实"、环节中文名、类型错误中文）；调用记录移除 `request_id` 原始 ID，只显示模型与环节。
- 修改对比：超过 3 处时折叠展示，界面保持整洁。
- 下载区：下载 MP4 改为醒目橙色按钮，海报链接并排（文本保持"下载MP4"兼容现有 QA 定位）。
- 证据页溢出滚动验证移入真实只读 QA；手机（390px）与平板（1024px）无横向溢出检查通过。

### QA 失败/重试检查修复（qa_studio.mjs，耗时排查）
- 现象：mock 模式下重试（复制为新项目 + 勾选自动检索）后 run 仍 blocked，检查超时。
- 根因链（三处叠加）：
  1. Mock 不执行自动检索是**设计行为**（`studio_pipeline.py`：`not settings.mock_ai` 才检索），重试成功必须手动补料；
  2. 资料名称 `required`（有正文时必须填），只填正文会被浏览器表单校验拦截 submit（不触发 onSubmit，无请求）；
  3. `getByLabel` 命中折叠 details 中的第二个 textarea（多元素等待超时），改用 `.studio-source textarea` 定位首个。
- 另修复：fill 触发受控组件 onChange 为异步批处理，立即提交会捕获陈旧 input（sources 被过滤为空），提交前等待 React 状态提交。
- 结果：mock QA 11 项检查全绿（含 `failure_state_shows_error`、`retry_recovers`），重试项目 `ee2b6cc2` sources:1 / versions:1 / run succeeded。

### 真实只读 QA（qa_studio_media.mjs，四道验收题）
- 播放、下载、海报、证据页独立滚动、手机无横向溢出、无页面错误、paid_actions:0 全部通过；输出目录 `evidence/studio-v050/browser-media`。

## LOG-003 自动补证据、直接视频与Windows封装候选（2026-09-01）

### 用户问题与根因
- 现象：用户只输入“什么是token”时，旧版只抓到一小段RFC或停在脚本/海报，页面要求用户补证据；同名测试项目大量堆积；旧版手动制片按钮在阻断状态不可点。
- 根因：模糊概念没有先区分常见含义；网页换行导致RFC完整句被切成小碎片；审核能列出问题但偶尔返回`revised:null`；warning与真正证据阻断没有完全分流；Windows启动器没有优先找到实际的`backend/.venv`。

### 解决
- `studio_research.py`：千问对模糊问题先扩展检索词；Token题读取IBM官方Tokenization页面与RFC 6749，所有摘录仍逐字回查网页，后备目录不是预写答案。网页视觉换行重新组合为可定位段落。
- `studio_store.py`：新增不可变`research_history`；重新检索追加尝试而不覆盖旧资料。新增可恢复项目归档表与管理脚本。
- `studio_pipeline.py`：审核有意见却漏写修订稿时，最多补一次“实际改稿”；连续只给建议时最多两轮并保留warning，warning继续视频，只有证据/科学blocker停止。
- `Studio.tsx`：默认提问后自动进入视频；旧任务手动制片仅作恢复。证据不足时提供“让千问扩展检索并自动重做视频”，不要求普通用户上传论文。
- 字幕：完整句保持同一cue，标点不单独换行；零模型调用重合成Token视频，旧MP4和改动原因保留。
- 清理：先制作私有完整备份，再将20个重复/相似失败项目归档，选择器仅保留6个代表项目；没有物理删除。

### Token真实端到端验收
- 最新请求`bae5ce7d-066c-4086-8a82-4fdf1914bda9`完成v13；媒体任务`702ea844-4863-52e8-88df-3b1512c5b133`自动完成6镜68秒MP4。
- 千问规划、Qwen-VL逐镜检查、Qwen TTS均有调用留痕；视频有音轨，12个完整句字幕块，三帧可解码。浏览器实际播放、下载、证据页、选做海报和手机宽度均通过，页面错误0、QA额外付费动作0。

### 排错记录
- 第一次浏览器QA误用了Playwright CommonJS入口，`chromium`为undefined；改用运行时`playwright/index.mjs`后通过。这是测试环境入口问题，不是应用故障。
- 完整测试最初发现“审核只提建议却不改稿”会产生无意义复检并耗尽模拟响应；改为只有实际修订稿（或无问题初稿）才复检，连续warning有界结束。最终169项全绿。
- PowerShell 5读取无BOM UTF-8中文状态文字发生乱码并破坏引号解析；启动脚本的早期状态行改用ASCII，避免不同系统代码页导致双击失败。

### 费用与备份
- 已知价格的本地台账累计约15.221339元；另有65条检索/重排调用须以百炼账单核对，仍未触发70元暂停线。
- 私有运行备份目录`scivis-runtime-20260901-173854`，ZIP 141844822字节，690文件，SQLite完整性OK，不含`.env`，SHA256：`5e9d2f55e52cc7c9521c0ad7cfce09c0391ed97696f020b2d52f525bf2fc514a`。

## LOG-004 v0.5.6 封装前跨设备与水色题验收（2026-09-01）

### 界面与可用性
- 用户指出浅色提示框中白字看不清，已改为深色文字、浅色背景与白底按钮；浏览器计算样式实测为`rgb(141,47,24)` / `rgb(255,240,233)`。
- 右侧不再使用独立长滚动容器，桌面端改为页面统一滚动，左侧粘性定位；390px手机页没有横向溢出。

### 资料检索与真实成片
- 扩大可信公开资料范围，仍拒绝个人博客和无编辑审核转载；新增有界的第二次摘录选择，允许保留已能支持部分核心主张的原文。
- 解决Python打包环境HTTPS证书链与系统浏览器不一致：改用`truststore`读取操作系统原生证书库，实测能读取NOAA与Science Learning Hub正文，证书/主机名校验保持开启。
- 水色项目`bcb58391`旧版为0来源；新请求`fae1b1e2`保存2份原文、无gap，自动生成v17与媒体`195523bd`。7镜68秒MP4，1280×720、音轨存在、15个完整句字幕cue、3帧抽样可解码。
- 浏览器验收实际播放成功，MP4 Range下载成功，证据页显示2份原文，页面错误0，额外付费动作0。

### 跨设备数据与运行候选
- `SCIENCE_POSTER_DATA_DIR`现在同时控制SQLite、媒体、音频、字幕和运行清单；运行备份工具也跟随该目录。
- 本地预检增加数据目录可写、系统CA依赖和macOS/Linux中文字体路径。Docker候选安装视频依赖、Noto CJK和ca-certificates，非root运行，使用`/data`持久卷。
- 当前主机没有Docker/Podman，未伪称已实际构建Linux镜像；明日在有Docker的环境或目标云构建再做最后验收。

### 测试与费用
- 后端194项通过，前端TypeScript/Vite生产构建通过，真实服务版本`0.5.6-preview`。
- 水色题运行前本地台账累计26.4096元；运行后已知价格的估算累计26.8433元（另65条检索/重排记录需以百炼账单核对），仍未触发70元暂停线。

## LOG-005 多案例端到端测试与桌面封装发布（2026-09-02，dev/general-science-video-v0.5）

### 多案例端到端测试：6 个科普问题全部产出视频
- 主题：蜜蜂嗡嗡声、铁生锈、火山形成、飞机升力、0 不能作除数、打哈欠传染。
- 结果 **6/6 passed**：直通 3 例（蜜蜂/火山/除零，run 两轮审核通过），run 证据阻断后人工确认制片 2 例（飞机/打哈欠），run 通过但媒体经修复后重试 1 例（铁）。
- 产物全部经 ffmpeg 探测 + 3 帧抽样：时长 68.0–73.12 秒，1280×720@20fps，音轨存在，帧可解码。
- 证据按例归档于 `evidence/multi-case-20260902/<项目前缀>/`（preview.mp4 + 3 帧截图）；多次运行的 summary.json 逐次覆盖，由新增 `scripts/merge_case_evidence.py` 重探测合并为最终验收记录（7 条记录，含首轮 EXE 打哈欠）。
- 阻断行为本身符合证据优先设计：资料不足的主题被审核闸门拦截（不产出无依据视频），人工确认后继续，全部路径最终仍得到成片。

### 测试发现与三处修复（全部进入最终构建）
1. **exchange 双向箭头被视觉检查误读**（打哈欠第 3/6/6 镜被阻断 3 次）：金色小箭头被 Qwen-VL 读成单向。修复 `studio_cartoon.py`：两端同色 teal 大箭头（◄──► 标准画法），流动圆点贴线对向往返，不再被读成线上未解释的节点。
2. **vision-repair 重规划引入未知 icon 导致整段媒体任务崩溃**（铁案例 `ValidationError: actors.2.icon`）：修复后 repair 路径与 plan 路径一致，先过 `normalize_actor_icons` 再校验。
3. **视觉审查忽略"简化示意"免责声明**（铁第 2 镜两次被阻断）：`studio_media.py` 审查提示增加子句——镜头已声明"简化示意/概念示意"时允许省略资料未覆盖的细节，科学误导指与已核实资料相矛盾而非资料未覆盖，避免无意义反复 revise。
- 后端测试增至 197 项全绿（新增箭头像素断言：两端都有 teal 箭头头、圆点在线内；新增 repair-icon 规范化测试）。

### 桌面封装发布（直接双击运行的桌面应用，非本地网页）
- PyInstaller onedir + pywebview(WebView2) 窗口应用；`console=False` 时 `sys.stdout=None` 会令 print 崩溃 → 入口 `redirect_std_streams` 修复；冻结路径经 `SCIVIS_ENV_FILE`/`SCIVIS_FRONTEND_DIST`/`SCIENCE_POSTER_DATA_DIR` 解析；构建脚本 `scripts/build_desktop.py`、启动器 `scripts/desktop_main.py`（--no-window QA 模式、端口预检、免费口 8765）。
- **最终 EXE（构建 b9hc3xzbt，含全部 3 处修复）端到端复验通过**：打哈欠案例 run=blocked（证据阻断）→ 人工确认 → media=succeeded，68.0 秒 MP4（1280×720，音轨齐全）。真实 `.env` 仅验证期间临时拷入、随后已从发布目录删除。
- 最终发布包：`dist-desktop/科学科普视频工作台-20260902-123144.zip`，SHA256 `959e2ae77977624564bd97829909242bb01e43203a8250fe1916e925290541eb`，377 条目。zip 审计：无 `.env`（仅 `.env.example` 模板 + `使用说明.txt`，字体走系统 msyh.ttc 无需打包），无 scivis-data/sqlite/日志；`build/`、`dist-desktop/` 已加入 .gitignore；三个过期中转 zip 已删除，仅保留最终包。

### 费用与台账
- dev 台账 35.05 元（1062 条，含本次 6 案例与重试的真实调用）+ EXE 最终验证 0.65 元 + EXE 早期测试台账丢失按类比补记 reconciliation 8.3 元（已追加至 `evidence/model-usage.jsonl`）≈ **合计约 44 元估算**，远低于 70 元暂停线/100 元上限；真实账单以百炼控制台为准。

### 遗留与后续
- 最终 zip 建议在装有 WebView2 的洁净 Windows 机器上双击做首次开箱验收（本机验证为同机 EXE 直跑）。
- 视频内容仍缺受众测试与领域专家终审，不得写为"已通过专家认证"。
