# 代码版本与交接记录

制作/最终分区与可点击标签见[版本导航](docs/versions/README.md)。目前无最终交付版；`main`仍是制作分支。

Git历史从2026-08-31首次导入开始。此前工作记录在工作区根目录的制作日志中，不能把本次导入说成过去已存在的Git提交。

| 标签 | 内容 | 边界 |
|---|---|---|
| `process/v0.5.0-preview` | 通用闭环四题端到端：JSON安全解析、确定性6镜降级、检索后备与部分支持选择、双向交换箭头渲染修正、媒体完整性复验；新增HTTP验收驱动与15项测试 | 制作预览；四道跨主题验收题均产出可播放MP4（68—91秒），内容仍须人工试听与科学终审；UI优化与Windows封装在后续里程碑 |
| `process/v0.4.5-handoff` | 新增下一阶段唯一执行说明，明确任意问题到视频、UI优化、Windows封装的修改文件、成果目录和验收规则 | 功能与v0.4.4相同；供Claude＋DeepSeek接手，仍非最终发布 |
| `process/v0.4.4-preview` | 修复Windows私有备份SQLite连接未关闭；失败目录标为不可恢复，正式包要求manifest、完整性和SHA256复核 | 稳定交接点；功能与v0.4.3相同，仍非最终发布 |
| `process/v0.4.3-preview` | 字幕按完整句计时并在同一cue内换行；增加Claude＋DeepSeek交接提示词和不含`.env`的私有运行数据备份工具 | 制作/交接预览；修正版真实成片已抽帧，仍须完整试听；第三方助手不得替代生产Qwen模型 |
| `process/v0.4.2-preview` | WAV按实际PCM计算时长、字幕时间轴硬校验；右侧真实制作进度与成片自动呈现，修订入口移至证据页 | 制作预览；修正版68秒视频已多时间点抽帧和浏览器验证，仍须完整人工试听与科学终审 |
| `process/v0.4.1-preview` | 卡通规划非法图标兼容、结构修复留痕；搜索异常仍读官方概念页，超长摘录保留合规前缀 | 制作预览；AI主题已实测68秒成片，API跨主题已验证8镜脚本但本轮未重复制片；仍须人工试听和科学终审 |
| `process/v0.4.0-preview` | 基础问题检索、详细讲解；默认卡通MP4＋AI旁白字幕，海报选做，检查与修订留痕 | 制作预览；千问规划＋程序动画，非视频模型；插画路线仍实验，仍须科学与视听终审 |
| `process/v0.3.0-preview` | 公众文案分层、解释型图解、6—8镜、问题自动检索入口及代码索引 | 制作预览；来源范围有限；通用有声卡通MP4尚未完成，见独立变更说明 |
| `v0.2.0-studio` | 跨主题资料隔离与独立项目、Qwen生成/审核/修订/复检、可视化修改记录、草稿ZIP和单端口本地启动 | 通用视频目前是独立分镜及无声预演，尚无自动MP4/配音；AI审核不代替科学终审；未公网部署 |
| `v0.1.0-baseline` | 导入现有海报原型、太阳有声样片、自动字幕/时长修正工作台；加入敏感文件排除与干净测试夹具 | 非任意主题全自动视频生成；没有公网部署 |
| `v0.1.1-handoff` | 补充GitHub建仓、接手运行、恢复版本与部署门槛说明 | 不含API密钥、在线知识库或私有原始成果档案 |
| `v0.1.2-handoff-check` | 根据新目录恢复测试补正“先构建前端，再跑后端测试”的顺序，记录实际验证环境 | 应用功能与上一版本相同；不伪装为新增功能版本 |

后续每个经过验证的功能形成新提交；重要里程碑增加新标签。不要移动已有标签、倒填提交时间或覆盖旧备份。代码仍未上传GitHub时，本地版本和离线备份可用于恢复，但不等同远端备份。

当前Git作者使用项目级临时身份 `SciVis Local Backup <scivis-backup@local.invalid>`，仅限本仓库，不代表用户的GitHub身份。接手者可在仓库内设置自己的提交姓名和邮箱；不要因此重写已有历史。

版本查看和安全恢复示例：

```powershell
git log --oneline --decorate
git tag
git switch -c review-baseline v0.1.0-baseline
```

切换前先保存当前改动；需要独立试验时优先从备份克隆到新目录，不要对工作目录执行强制重置。
